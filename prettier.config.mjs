// Prettier 配置文件
export default {
  // 缩进的宽度（以空格为单位）
  tabWidth: 2,
  // 是否使用 Tab 键进行缩进
  useTabs: false,
  // 使用单引号还是双引号
  singleQuote: true,
  // 行结束符的类型
  endOfLine: 'auto',
  // JSX 中是否使用单引号
  jsxSingleQuote: true,
  // 最大行宽
  printWidth: 80,
  // 是否在语句末尾打印分号
  semi: true,
  // 在对象字面量中是否使用空格
  bracketSpacing: true,
  // JSX 的闭合标签是否在同一行
  jsxBracketSameLine: false,
  // 箭头函数的参数括号的显示方式
  arrowParens: 'always',
  // 是否使用尾随逗号
  trailingComma: 'all',
  // 控制是否每个属性都单独占一行
  singleAttributePerLine: true,
};
